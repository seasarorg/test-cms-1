package org.seasar.cms.wiki.visitor;

import org.seasar.cms.wiki.engine.impl.WikiEngineTestFramework;

public class PdfWikiVisitorTest extends WikiEngineTestFramework {

	/*
	 * public void testMerge() throws Exception { FileOutputStream out = new
	 * FileOutputStream("target/test.pdf");
	 * 
	 * WikiContext context = new WikiContext(); context.setOutputType("pdf");
	 * engine.merge("*hoge\n abc", context, out); }
	 */
}
